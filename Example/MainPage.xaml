<Page
    x:Class="Example.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Example"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="MainPage_OnLoaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <GridView ItemsSource="{Binding list}" x:Name="GridViewShow">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="450" Height="450" Padding="5">
                        <TextBlock FontWeight="Bold" FontSize="16" Name="TxtVideo" Text="{Binding videoId}"/>
                        <TextBlock FontWeight="Bold" FontSize="24" Name="TxtName" Text="{Binding name}"/>
                        <TextBlock TextWrapping="Wrap" Name="TxtDescription" Text="{Binding description}"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
